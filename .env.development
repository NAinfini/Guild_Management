# Development environment variables
# Use relative /api path - Vite proxy will forward to production worker
# The Vite proxy is configured in vite.config.ts for local testing

# VITE_API_BASE_URL=/api (default - no need to set explicitly)
VITE_WS_URL=wss://guild-management.na-infini.workers.dev
