# Development environment variables
# In development, we still want to use the actual Workers endpoint
# The Vite proxy is configured in vite.config.ts for local testing

VITE_API_BASE_URL=https://guild-management.na-infini.workers.dev
VITE_WS_URL=wss://guild-management.na-infini.workers.dev
