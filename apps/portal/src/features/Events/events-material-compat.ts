/**
 * Transitional material compatibility surface for the Events page.
 * Keep direct compat imports out of the page module while migration proceeds in slices.
 */
export { useTheme, alpha } from '@/ui-bridge/material';
